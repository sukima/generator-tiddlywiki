{
  "name": "TiddlyWiki",
  "version": "0.0.0",
  "description": "a non-linear personal web notebook",
  "private": true,
  "dependencies": {
    "tiddlywiki": "~5.0.9-beta"
  },
  "devDependencies": {
    <% if (isDeployable) { %>
    "scp": "0.0.3",
    <% } %>
    "gulp": "~3.6.0",
    "gulp-util": "~2.2.14",
    "gulp-clean": "~0.3.1"
  }
}
